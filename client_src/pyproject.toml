[project]
name = "signal-client"
version = "0.1.0"
description = "Public client-mode Signal portfolio audit runner"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
